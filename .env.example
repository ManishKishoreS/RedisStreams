# Database (PostgreSQL)
DATABASE_URL="postgresql://user:password@localhost:5432/retirewise?schema=public"

# Authentication (Clerk) — optional for the engine/dashboard demo
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=""
CLERK_SECRET_KEY=""
